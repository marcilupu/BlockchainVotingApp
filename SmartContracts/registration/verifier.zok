def main(private field birthYear, private field userCounty) {
	field currentYear = 2020;
	field legalVotingAge = 18;
	field electionCounty = 1;

	field age = currentYear - birthYear;

	assert(age >= legalVotingAge && age > 0, "Age is not within legal limits");
    	assert(userCounty == electionCounty, "The user is not located in the county of the election.");

	return;
}