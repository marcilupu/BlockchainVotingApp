def main(private field userId) {
	field[5] ids = [3,1,2,4,5];
	field randomSeed = 857701019;
	field mut match = randomSeed;
	for u32 i in 0..5 {
		match = if ids[i] == userId { match + 1 } else { match }; 
	}
	assert(match > randomSeed);
	return;
}
